name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docker:
    name: Build and Push
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Validate semver tag
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          # Semver regex: X.Y.Z or X.Y.Z-prerelease or X.Y.Z-prerelease.N
          if [[ ! "$TAG" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[a-zA-Z]+(\.[0-9]+)?)?$ ]]; then
            echo "::error::Invalid semver tag: $TAG"
            echo "Expected format: vX.Y.Z, vX.Y.Z-prerelease, or vX.Y.Z-prerelease.N (e.g., v0.1.0, v0.1.0-beta, v0.1.0-rc.2)"
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v5

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create gradle.properties for build
        run: |
          echo "repoUsername=${{ secrets.REPSY_USERNAME }}" > gradle.properties.secret
          echo "repoPassword=${{ secrets.REPSY_TOKEN }}" >> gradle.properties.secret

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ steps.meta.outputs.version }}
          secret-files: |
            gradle_props=./gradle.properties.secret
          cache-from: type=gha
          cache-to: type=gha,mode=max
